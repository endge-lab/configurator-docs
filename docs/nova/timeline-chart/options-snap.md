# snap

`snap` округляет время при drag/edit. Используйте его, когда задача должна двигаться по сетке: 5 минут, 15 минут, час или рабочая смена.

```ts
snap: time => Math.round(time / (15 * 60 * 1000)) * 15 * 60 * 1000
```
