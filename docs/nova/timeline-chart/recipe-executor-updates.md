# Частые updates с executor

Настройте `executor.delay` и `executor.maxDelay`, если updates приходят чаще, чем нужен визуальный refresh.

:::example id="timeline-chart-live-updates" layout="tabs"
:::
