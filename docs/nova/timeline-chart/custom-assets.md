# Assets

`assets` передаются Vue-компоненту и используются Nova runtime для preload изображений, иконок и текстур.

## Рекомендация

Держите assets рядом с feature, которая владеет template. Timeline не должен знать, откуда бизнес-слой получает изображения.
