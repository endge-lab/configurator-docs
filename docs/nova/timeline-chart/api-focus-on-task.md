# focusOnTask

`focusOnTask(taskId, options)` переводит viewport к задаче и возвращает `false`, если id не найден.

```ts
timeline.value?.focusOnTask('task-1', {
  vAlign: 'center',
  hAlign: 'center',
  select: true,
})
```

:::example id="timeline-chart-api-updates" layout="tabs"
:::
