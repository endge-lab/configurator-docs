# timeScale

`timeScale` управляет верхней шкалой времени: высотой, форматами и плотностью ticks.

## Когда настраивать

Меняйте формат шкалы, когда один и тот же timeline может показывать минуты, часы, дни или месяцы. Для обычного hourly-view чаще достаточно locale/timezone.
