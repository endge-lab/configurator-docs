# Custom overlays

Custom overlay plugin нужен, когда поверх timeline нужно показать SLA bands, external cursor, dependency lines или аналитические подсветки.

## Рекомендация

Overlay должен читать viewport/runtime context и рисовать только видимую часть.
