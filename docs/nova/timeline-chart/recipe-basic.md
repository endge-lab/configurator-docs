# Одна группа и одна задача

Минимальный рецепт: один group object, одна range task и `TimelineChart`.

:::example id="timeline-chart-basic" layout="tabs"
:::

## Код

Откройте вкладку `Код`: пример содержит только typed data и импорт из `@engine2d/timeline-chart`.
