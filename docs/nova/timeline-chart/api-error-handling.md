# Error handling

Публичный API должен получать валидные id и корректные временные интервалы.

## Практика

- Проверяйте `endTime > startTime` в бизнес-слое.
- Обрабатывайте `false` от `focusOnTask`.
- Для неизвестных id в `update` логируйте прикладной warning до вызова API.
